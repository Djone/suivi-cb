# Fichier d'exemple de variables d'environnement
# Copiez ce fichier en .env.production et adaptez les valeurs

# ============================================
# CONFIGURATION BACKEND
# ============================================

# Environnement (development, production)
NODE_ENV=production

# Port du serveur backend
PORT_BACK=3001

# Chemin vers la base de données SQLite
# En production Docker: /app/data/database.db
# En local: ./backend/database.db
DB_PATH=/app/data/database.db

# ============================================
# CONFIGURATION FRONTEND
# ============================================

# Port du serveur frontend (dans le container nginx)
PORT_FRONT=80

# URL de l'API backend
# En production Docker: http://backend:3001 (nom du service Docker)
# En local: http://localhost:3000
API_URL=http://backend:3001

# ============================================
# CONFIGURATION SYNOLOGY NAS (pour déploiement)
# ============================================

# Informations de connexion au NAS
NAS_USER=admin
NAS_HOST=192.168.1.100
NAS_SSH_PORT=22
NAS_APP_DIR=/volume1/docker/suivi-cb

# Domaine public de l'application
APP_DOMAIN=finances.votredomaine.com

# ============================================
# SÉCURITÉ (optionnel - à implémenter)
# ============================================

# Clé secrète pour JWT (si authentification future)
# JWT_SECRET=votre_secret_tres_long_et_complexe

# CORS - Origines autorisées
# CORS_ORIGIN=https://finances.votredomaine.com

# ============================================
# LOGS (optionnel)
# ============================================

# Niveau de log (error, warn, info, debug)
LOG_LEVEL=info

# Dossier des logs
LOG_DIR=/app/logs

# ============================================
# BACKUPS (optionnel)
# ============================================

# Dossier des sauvegardes
BACKUP_DIR=/volume1/Backups/suivi-cb

# Nombre de jours de rétention des backups
BACKUP_RETENTION_DAYS=30

# ============================================
# NOTES
# ============================================

# Ce fichier ne doit JAMAIS être commité dans Git
# Ajoutez-le au .gitignore

# Pour utiliser ces variables:
# 1. Copiez ce fichier: cp .env.example .env.production
# 2. Modifiez les valeurs selon votre configuration
# 3. Assurez-vous que .env.production est dans .gitignore
